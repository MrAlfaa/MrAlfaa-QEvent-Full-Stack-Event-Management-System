# JWT Configuration
security.jwt.secret-key=cssz3izcML7XwKWhCxijOjzL9rMzMA4jzUGs6uzE25nusEnAE7zJUPSW47hLYyhb79Fo0yYHc

# Database Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/qevent
spring.datasource.username=postgres
spring.datasource.password=admin

# JPA Configuration
spring.jpa.hibernate.ddl-auto=create
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.temp.use_jdbc_metadata_defaults=false

# Uncomment the following line to show SQL queries in logs
# spring.jpa.show-sql=true

# Mail Configuration
# TODO: Add your mail server details
# spring.mail.host=
# spring.mail.port=
# spring.mail.username=
# spring.mail.password=

# Additional configurations for QEvent system
spring.application.name=QEvent-Management-System
server.port=8080

# Logging configuration
logging.level.com.mralfaa.qevent=DEBUG

# Actuator endpoints for monitoring
management.endpoints.web.exposure.include=health,info,metrics

# File upload configuration
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Security configuration
spring.security.user.name=admin
spring.security.user.password=adminpassword

# Timezone configuration
spring.jackson.time-zone=UTC
